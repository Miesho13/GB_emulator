#include <stdio.h>
#include <stdlib.h>

#include "GB_int.h"
#include "GB.h"

int main(int argc, char **argv) {
    (void)argc;
    (void)argv;
    
    printf("[INFO] Start emulation.\n");
//  load_program(...);
    while(1) {
//      step(...);
        break;
    }
    return 0;
}
